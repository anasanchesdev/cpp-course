#include <iostream> 
#include <cmath>

/*


*/

int main(){
    using namespace std;

    double x = 3.5;
    double y = 5;
    double z;

    // Encontra valor máximo entre dois valores
    // z = max(x, y);

    // Encontra valor mínimo entre dois valores
    // z = min(x, y);

    // Função de potenciação pow(base, expoente)
    // z = pow(x, y);

    // Função de raiz quadrada
    // z = sqrt(16);

    // Função de módulo
    // z = abs(-10);

    // Arredondamento (>= x.5 pra cima, < x.5 pra baixo)
    // z = round(x);

    // Arredondamento sempre pra cima
    // z = ceil(x);

    // Arredondamento sempre pra baixo
    // z = floor(x);

    cout << z;
    return 0;
}