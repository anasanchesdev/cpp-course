#include <iostream>

int main(){

    // int (números inteiros)
    int idade = 13;

    // double (números decimais)
    double temperatura = 23.3;

    // bool (true ou false)
    bool vivo = false;

    // char (caracter único)
    char nota = 'A';

    // string (sequencia de caracteres)
    std::string nome = "Ana";

    // Concatenando variáveis
    std::cout << "Ola " << nome << ", tudo bem?" << '\n';
    std::cout << "Voce tem " << idade << " anos?"; 
    
    return 0;
}
